(defun doublenumber (n)
  "Doubles the number passed to the function (non-interactive)"
  (* n 2)
)

(doublenumber 6)

(defun idoublenumber (n)
  "Doubles the number passed to the function (interactive)"
  (interactive "p")
  (message "2 * %d = %d" n (* n 2))
)




